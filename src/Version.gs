/**
 * Versão exibida pela interface.
 * Atualizada automaticamente pelo comando oficial de publicação.
 */
const APP_VERSION = "2.3.174";
const APP_VERSION_DISPLAY = "2.3.174";

function obterVersaoAplicacao() {
  return APP_VERSION;
}
